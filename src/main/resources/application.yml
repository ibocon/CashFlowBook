# Secret
jasypt:
  encryptor:
    password: ${SECRET}
    bean: jasyptEncryptor

# PostgresSQL
spring:
  datasource:
    url: ${URL}
    username: postgres
    password: ENC(WywYnHdKMc9rUB3eYXEGAW8qkP/80vBA)
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    open-in-view: false
